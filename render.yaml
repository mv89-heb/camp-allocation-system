services:
  - type: web
    name: camp-allocation-system
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bootstrap_neon.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
